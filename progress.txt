## Iteration 1 — AUDIT-1: Backend Services Audit
**Task:** Map all 37 service files and 198+ functions to their contract review workflows.
**Files Changed:** AUDIT_MAP.md (created), REFACTOR_PRD.md (checked off)
**What I learned:**
- 37 service files with 198+ public functions organized across 25 workflow categories
- Core pipeline: gemini_analyzer.py -> analysis_pipeline.py -> rule_engine.py + rules_library.py -> hallucination_guard.py -> confidence_scorer.py -> redline_implementer.py
- AI providers: vertex_client.py (Vertex AI) -> gemini_analyzer.py (consumer Gemini) -> ai_service.py (Azure OpenAI fallback)
- Key auxiliary services: scope_analyzer, jurisdiction_detector, defined_terms_resolver, dependency_resolver
- analysis_pipeline.py is the orchestrator -- it calls gemini_analyzer, hallucination_guard, confidence_scorer, scope_analyzer, jurisdiction_detector
- intelligence_bridge.py provides Strategy pattern (OmniContext for small docs, HybridSentinel for large) but may not be actively used since gemini_analyzer.py handles the main flow now
- rules_library.py has 75 built-in rules for clause detection -- this is the default playbook
- playbook_conditions_engine.py handles conditional rule overrides based on deal context
- text_normalizer.py is critical for Word text matching (smart quotes, soft hyphens, zero-width chars)
- redline_implementer.py generates surgical word-level OOXML Track Changes using SequenceMatcher

**Patterns for next iteration:**
- Service files are well-organized by concern. Most are actively imported by other services or endpoints.
- The lazy import in __init__.py means services load on demand -- don't break this.
- playbook_cache.py provides in-memory caching for rule engines -- critical for performance.

## Iteration 2 — AUDIT-2: Backend Endpoints Audit
**Task:** Map all 13 endpoint files (105+ routes) to services and frontend callers.
**Files Changed:** AUDIT_MAP.md (appended), REFACTOR_PRD.md (checked off)
**What I learned:**
- 105+ routes across 13 endpoint files. ALL routes are connected to frontend callers.
- 0 DISCONNECTED routes, 0 UNEXPOSED service functions.
- documents.py is the largest with 24 routes covering the full analysis pipeline.
- The analysis flow: POST /analyze-full -> analysis_pipeline.run() -> gemini_analyzer -> hallucination_guard -> confidence_scorer
- Billing has webhook endpoints (razorpay/stripe) that are external-only (correct behavior).
- SSO endpoints exist but require WorkOS credentials to function.
- Playbooks have 19 routes covering full CRUD + rules + tiers + conditions + dependencies + versioning + marketplace.
- Key insight: The codebase is remarkably well-wired. Most "disconnected" perception comes from functions being called through import chains (service A calls service B) rather than directly from endpoints.

## Iteration 3 — AUDIT-3: Dashboard React Components
**Task:** Map all 19 page components + 1 shared component.
**Files Changed:** AUDIT_MAP.md (appended), REFACTOR_PRD.md (checked off)
**What I learned:**
- 19 page components, all routed in App.tsx. 0 orphaned components.
- ForgotPassword.tsx has a TODO -- not wired to backend /auth/forgot-password. This is a gap to fix later.
- PlaybookEditor.tsx is the most complex page, importing 16+ API functions for rules/tiers/conditions/overrides/dependencies/versions.
- Analytics.tsx has 4 sub-tabs (overview, portfolio, team, reports) -- very feature-rich.
- All components use @tanstack/react-query for data fetching -- consistent pattern.
- useFocusTrap hook used for modal accessibility in Playbooks.tsx.

## Iteration 4 — AUDIT-4: Word Add-in Functions
**Task:** Map all api.ts methods and taskpane.ts functions.
**Files Changed:** AUDIT_MAP.md (appended), REFACTOR_PRD.md (checked off)
**What I learned:**
- api.ts has 28 public methods. 10 are not called from taskpane.ts (playbook CRUD, clause library, getCurrentUser, generateClause). These are shared with dashboard pattern but intentionally dashboard-only features.
- taskpane.ts has 5 exported functions + ~30 internal functions. All are called via event handlers or other functions.
- The scan flow is: scanDocument() -> api.analyzeWithAI() -> displayAIResults() -> renderRedlineList() -> createAIRedlineCard()
- The fix flow is: Generate Fix button -> api.generateFix() -> word diff preview -> Apply button -> api.generateRedlineZDR() -> insertOoxml()
- Negotiation mode is fully wired: toggleNegotiationMode() -> accept/counter/escalate buttons -> recordNegotiationDecision()
- The bug: exportReport() uses this.accessToken which doesn't exist. Should use cookie-based auth. Fix in Phase 3.

## Iteration 5 — AUDIT-5: 5-Stage Analysis Pipeline Trace
**Task:** Trace every stage, verify all imports, identify broken links.
**Files Changed:** AUDIT_MAP.md (appended), REFACTOR_PRD.md (checked off)
**What I learned:**
- Pipeline is fully wired. All 5 stages execute correctly with graceful degradation.
- Stage 1 (Extraction): StructureExtractor + DefinedTermsResolver + JurisdictionDetector -- all deterministic, no AI cost
- Stage 2 (Classification): RuleEngine.evaluate() -- pattern matching against 75 built-in rules or custom playbook rules
- Between 2-3: ScopeAnalyzer runs scope analysis + jurisdiction overrides
- Stage 3 (Risk Assessment): GeminiAnalyzer.analyze_full_contract() -- the heavy AI call (Gemini Pro)
- Stage 4 (Verification): HallucinationGuard.verify_batch() -- kills AI hallucinations via 4-tier text matching
- Stage 5 (Enrichment): ConfidenceScorer.score_batch() -- 5-factor scoring (0-1) per redline
- Key insight: Stage 3 has a fallback to rule-engine-only mode if AI is unavailable. This means the system degrades gracefully without Gemini API access.

## Iteration 6 — AUDIT-6: AI Provider Chain
**Task:** Map vertex_client.py -> gemini_analyzer.py -> ai_service.py provider chain.
**Files Changed:** AUDIT_MAP.md (appended), REFACTOR_PRD.md (checked off)
**What I learned:**
- vertex_client.py has clean thread-safe double-checked locking for backend selection
- Vertex AI (enterprise) -> Consumer Gemini (dev) -> None (error). REQUIRE_VERTEX_AI blocks consumer fallback.
- ai_service.py has Azure OpenAI as a separate fallback but it's NOT used by the main pipeline (analysis_pipeline.py uses gemini_analyzer directly)
- No broken links in provider chain.

## Iteration 7 — AUDIT-7: Redline Generation Pipeline
**Task:** Map the full redline chain from Generate Fix to Track Changes in Word.
**Files Changed:** AUDIT_MAP.md (appended), REFACTOR_PRD.md (checked off)
**What I learned:**
- Full chain: Generate Fix -> generate_fix() -> FixVerifier -> word diff preview -> Apply -> generateRedlineZDR -> RedlineImplementer.find_anchor() -> generate_track_changes_ooxml() -> insertOoxml()
- find_anchor() uses 3 strategies: hash match (SHA-256) -> exact substring -> fuzzy (rapidfuzz, 80% threshold)
- generate_track_changes_ooxml() uses difflib.SequenceMatcher for word-level diffs -- NOT bulk paragraph replacement
- Missing clause handling: generate_insert_only_ooxml() with InsertLocation.after
- Word Add-in uses insertOoxml() for both violations (replace) and missing clauses (after)
- No broken links. Pipeline is fully wired.

## Iteration 14 — VERIFY-1 through VERIFY-8: Final Verification + FINAL_STATUS.md
**Task:** Run all build commands, trace end-to-end flows, create FINAL_STATUS.md.
**Bug Fixed:** exportReport() in api.ts used dead this.accessToken. Replaced with cookie-based CSRF auth.
**Build Results:** All 5 pass (backend imports, dashboard TS + build, add-in TS + webpack build)
**Files Changed:** ContraRed-PoC/src/taskpane/api.ts (bug fix), FINAL_STATUS.md (created), REFACTOR_PRD.md (all checked)

## Iteration 13 — HARDEN-1 through HARDEN-10: Hardening Verification
**Task:** Verify all 10 hardening requirements.
**What I learned:**
- Global exception handler in main.py catches all unhandled exceptions with PII redaction
- Pydantic models used extensively (35 in documents.py, 10 in auth.py)
- CORS: production uses explicit CORS_ORIGINS, DEBUG uses regex (correct behavior)
- No hardcoded secrets found anywhere
- Rate limiting on auth (8) and documents (11) endpoints
- Token blacklist: Redis + in-memory fallback in token_service.py
- RLS: TenantContextMiddleware sets PostgreSQL session vars from JWT
- CSP: netlify.toml allows contrared.onrender.com and contrared-api.onrender.com

## Iteration 12 — SUPPORT-1 through SUPPORT-12: Supporting Features Verification
**Task:** Verify all 12 supporting features.
**What I learned:** All supporting features verified as wired:
- Playbook CRUD (9 calls in PlaybookEditor, 3 in Playbooks)
- Versioning (4 calls in PlaybookEditor)
- Marketplace (4 calls in Marketplace.tsx)
- Clause Library (5 calls in ClauseLibrary.tsx)
- Team management (4 calls in Team.tsx)
- Billing (5 calls in Billing.tsx)
- Audit logs (2 calls in AuditLogs.tsx)
- Analytics (14 calls in Analytics.tsx)
- Recent scans (3 calls in taskpane.ts)
- SSO: endpoints exist, require WorkOS credentials (documented)
- Feedback: 3 endpoints exist, no dedicated dashboard page (documented)
- No orphaned UI components

## Iteration 11 — REDLINE-1 through REDLINE-7: Redline & Fix Pipeline Verification
**Task:** Verify all redline/fix features end-to-end.
**What I learned:** All 7 redline tasks verified as fully wired:
- Generate Fix: POST /generate-fix -> analysis_pipeline.generate_fix() -> FixVerifier
- Apply Fix: POST /redline (ZDR) -> RedlineImplementer -> OOXML -> insertOoxml()
- Text anchoring: hash -> exact -> fuzzy (rapidfuzz 80% threshold)
- Apply All: applyAllRedlines() iterates unfixed risks sequentially
- Fixed/Undo: fixedRisks Set + localStorage persistence
- Negotiation: toggleNegotiationMode() -> accept/counter/escalate -> session in localStorage
- Export: POST /export-report -> generate_risk_report() -> DOCX blob download

## Iteration 10 — ANALYZE-1 through ANALYZE-8: Document Analysis Pipeline Verification
**Task:** Verify the entire document analysis pipeline end-to-end.
**What I learned:**
- ANALYZE-1: Document upload via BatchUpload.tsx -> POST /batch-analyze. Word Add-in reads text directly from Word via getDocumentText(). No separate upload page (intentional design).
- ANALYZE-2: POST /analyze-full -> analysis_pipeline.run() -> 5 stages -> Document stored. 120s timeout. Fully wired.
- ANALYZE-3: scanDocument() -> api.analyzeWithAI() -> POST /analyze-full -> displayAIResults() -> risk cards. Fully wired.
- ANALYZE-4: scanSelection() -> api.analyzeClause() -> POST /analyze-clause. Fully wired.
- ANALYZE-5: RuleEngine() loads ALL_RULES from rules_library.py (75 rules). Stage 2 calls evaluate(). Fully wired.
- ANALYZE-6: Stage 4 runs HallucinationGuard.verify_batch() -- NOT bypassed. Fully active.
- ANALYZE-7: Playbook loaded via load_playbook() -> rules passed to pipeline.run(). Fully wired.
- ANALYZE-8: Code trace confirms full pipeline. Production test requires manual verification.

## Iteration 9 — AUDIT-9: Disconnected Functions
**Task:** Create DISCONNECTED_FUNCTIONS.md listing unreachable functions.
**Files Changed:** DISCONNECTED_FUNCTIONS.md (created), REFACTOR_PRD.md (checked off)
**What I learned:**
- Only 6 backend functions are truly disconnected (no callers): suggest_fix_with_playbook, classify_batch, get_playbook_by_industry, get_default_playbook_data, get_requery_instruction, normalize_for_search
- Word Add-in has ~10 API methods that are dashboard-only (intentional, not bugs)
- ForgotPassword.tsx uses a stub instead of calling the backend endpoint (gap to fix)
- The codebase is remarkably well-wired for its size. The disconnected functions are minor and represent planned features or optimizations not yet integrated.

## Iteration 8 — AUDIT-8: Async Task System
**Task:** Map TaskQueue, AnalysisJob lifecycle, frontend polling.
**What I learned:** TaskQueue uses Redis lists (or in-memory dict fallback). JobStatus: QUEUED→RUNNING→COMPLETED/FAILED. Frontend polls GET /documents/jobs/{job_id}. Fully wired.

## Iteration 15 — REGEX-1: rule_engine.py Full Audit
**Task:** Read all 730 lines of `backend/app/services/rule_engine.py`. Document every regex pattern and rule. Classify each as KEEP/REPLACE/REMOVE.
**File:** `backend/app/services/rule_engine.py` (730 lines)

### File Structure
- **Lines 1-26**: Imports + logger
- **Lines 29-33**: `RiskLevel` enum (RED/YELLOW/GREEN)
- **Lines 36-51**: `RulePattern` dataclass (v1 rule format)
- **Lines 53-68**: `SmartRule` dataclass (v2, extends RulePattern with negative_patterns, escalation/de-escalation)
- **Lines 71-94**: `RuleMatch` dataclass (output of matching, includes cache_key for AI responses)
- **Lines 97-113**: Risk escalation/de-escalation helpers (`_escalate_risk`, `_deescalate_risk`)
- **Lines 120-312**: `DEFAULT_RULES` list — 13 built-in rules (not 16 as PRD states)
- **Lines 315-730**: `RuleEngine` class

### All 13 DEFAULT_RULES — Regex Patterns Inventory

**1. `unlimited_liability` (RED, deal_breaker=True)**
- `\b(unlimited\s+liability)\b`
- `\b(no\s+(cap|limit|limitation)\s+(on|to)\s+liability)\b`
- `\b(without\s+limit(ation)?\s+of\s+liability)\b`
- `\b(liability\s+shall\s+not\s+be\s+limited)\b`
- **Classification: REPLACE-WITH-AI** — These catch obvious phrases but miss nuanced unlimited liability clauses. E.g., "The Vendor's aggregate liability hereunder shall not be subject to any monetary limitation" would NOT match any pattern. AI understands semantic equivalents.

**2. `unilateral_termination` (RED, deal_breaker=True)**
- `\b(terminate\s+(this\s+)?(agreement|contract)\s+(at\s+)?any\s+time)\b`
- `\b(termination\s+without\s+(cause|reason))\b`
- `\b(may\s+terminate\s+(immediately|forthwith)\s+without\s+(notice|cause))\b`
- **Classification: REPLACE-WITH-AI** — Misses "Either party reserves the right to discontinue this arrangement at its sole discretion" — semantically identical but uses different vocabulary. Also can't distinguish buyer vs seller termination rights (context-dependent risk).

**3. `broad_indemnification` (SmartRule, base YELLOW, can escalate to RED)**
- Positive patterns (4): `indemnify and hold harmless`, `defend, indemnify, hold harmless`, `indemnif(y|ication) against any and all`, `unlimited indemnif(y|ication)`
- Negative patterns (1): `mutual(ly)?.{0,80}limited\s+to` — suppresses if mutual+limited
- Escalation (3): `one-sided|unilateral`, `uncapped|without cap|no cap`, `any and all (loss|damage|claim|liabilit)`
- De-escalation (1): `mutual(ly)?.{0,120}capped? (at|to)`
- **Classification: REPLACE-WITH-AI** — This is the most sophisticated rule (SmartRule with escalation logic), but still can't handle: (a) clauses that describe indemnification without using the word "indemnify", (b) cross-reference indemnification ("as set forth in Section 8.2"), (c) carve-outs that change the risk profile.

**4. `ip_assignment` (RED, deal_breaker=True)**
- `\b(work(s)?\s+for\s+hire)\b`
- `\b(assign(s|ment)?\s+(of\s+)?all\s+(intellectual\s+property|IP))\b`
- `\b(all\s+(rights|IP|intellectual\s+property)\s+shall\s+(belong|vest)\s+to)\b`
- **Classification: REPLACE-WITH-AI** — Misses "any developments, inventions, or improvements made during the term shall become the exclusive property of Client" — no "work for hire" or "assign" keyword.

**5. `auto_renewal` (YELLOW)**
- `\b(auto(matic(ally)?)?[\s-]?renew(al|s|ed)?)\b`
- `\b(shall\s+(automatically\s+)?renew\s+for\s+(successive|additional))\b`
- `\b(renew(s|ed)?\s+automatically)\b`
- **Classification: KEEP** — Auto-renewal uses very standard language. Regex is fast and accurate here. False negatives unlikely.

**6. `assignment_restriction` (YELLOW)**
- `\b(may\s+not\s+assign)\b`
- `\b(no\s+assignment\s+without\s+consent)\b`
- `\b(assignment\s+(is\s+)?(prohibited|restricted))\b`
- **Classification: REPLACE-WITH-AI** — Needs context: "may not assign without prior written consent, such consent not to be unreasonably withheld" is standard and fine. "may not assign under any circumstances" is problematic. Regex can't distinguish.

**7. `non_compete` (YELLOW)**
- `\b(non[\s-]?compete)\b`
- `\b(shall\s+not\s+compete)\b`
- `\b(restriction\s+on\s+competition)\b`
- **Classification: REPLACE-WITH-AI** — Needs contextual analysis of scope, duration, geography. "Non-compete limited to 6 months in Mumbai" vs "Non-compete worldwide for 5 years" are drastically different risks.

**8. `exclusive_dealing` (YELLOW)**
- `\b(exclusive\s+(right|license|agreement))\b`
- `\b(sole\s+and\s+exclusive)\b`
- `\b(exclusivity\s+(period|term|clause))\b`
- **Classification: REPLACE-WITH-AI** — "Exclusive right" could be perfectly fine (exclusive license to use software) or dangerous (exclusive dealing arrangement). Context-dependent.

**9. `confidentiality_term` (YELLOW)**
- `\b(perpetual\s+confidentiality)\b`
- `\b(confidential(ity)?\s+(in\s+)?perpetuity)\b`
- `\b(survive\s+(indefinitely|forever|in\s+perpetuity))\b`
- **Classification: KEEP as pre-filter** — The keyword "perpetual" or "perpetuity" is a strong signal. But AI should evaluate whether perpetual confidentiality is actually problematic in context (trade secrets vs general business info).

**10. `non_solicitation` (YELLOW)**
- `\b(non[\s-]?solicit(ation)?)\b`
- `\b(shall\s+not\s+solicit)\b`
- `solicit.{0,100}(employee|personnel|staff|contractor)`
- `\b(solicit,?\s+(induce,?\s+)?(recruit|hire|employ))\b`
- `\b(recruit(ing)?\s+(any\s+)?employee)\b`
- **Classification: KEEP as pre-filter** — Keyword-based detection is appropriate. AI should evaluate scope/duration.

**11. `reverse_engineering` (YELLOW)**
- `\b(reverse\s+engineer(ing)?)\b`
- `\b(decompil(e|ation)|disassembl(e|y))\b`
- `\b(derive\s+source\s+code)\b`
- **Classification: KEEP** — Very specific technical terms. Regex is perfect here.

**12. `governing_law` (GREEN)**
- `\b(govern(ed|ing)\s+(by|law))\b`
- `\b(laws\s+of\s+(the\s+)?(state|country|jurisdiction))\b`
- `\b(applicable\s+law)\b`
- **Classification: KEEP** — Standard clause detection. GREEN risk = informational only. Regex appropriate.

**13. `notice_provision` (GREEN)**
- `\b(notice(s)?\s+(shall|must|should)\s+be\s+(sent|given|provided))\b`
- `\b(written\s+notice)\b`
- **Classification: KEEP** — Standard clause detection. GREEN risk = informational only.

### RuleEngine Class Methods Analysis

**`__init__` (lines 328-390)** — KEEP
- Loads rules (custom, expanded 75-rule library, or default 13)
- Compiles all regex patterns at init time (good for performance)
- Handles SmartRule negative/escalation patterns
- Robust error handling for invalid regex

**`evaluate` (lines 392-513)** — KEEP (but role should change)
- Normalizes text via `normalize_text()` before matching
- For each rule: finds all regex matches, applies negative suppression, expands to sentence, deduplicates
- SmartRule: checks escalation/de-escalation in context window
- **Key insight**: This should become a PRE-FILTER that feeds candidates to AI, not a standalone risk assessor. Currently its output goes directly to results.

**`_remove_overlapping_matches` (lines 515-547)** — KEEP
- Deduplicates within same rule using 100-char proximity threshold
- Good heuristic for sentence-level dedup

**`_dedupe_cross_rule_matches` (lines 549-566)** — KEEP
- Prevents same text being flagged by multiple rules
- Uses normalized text comparison

**`_expand_to_sentence` (lines 568-608)** — KEEP
- Expands regex match to surrounding sentence (max 300 chars)
- Sentence boundary detection via `.!?;`
- **Issue**: `;` as sentence boundary is debatable in legal text where semicolons separate list items within a single clause. Could cause truncated context.

**`get_risk_summary` (lines 610-622)** — KEEP (utility)

**`get_default_rules` (lines 624-627)** — KEEP (utility)

**`from_playbook_rules` (lines 629-730)** — KEEP (but needs migration path)
- Converts PlaybookRule DB objects into RulePattern/SmartRule instances
- Supports 3 match_type modes: `exact` (escapes regex chars), `fuzzy` (word boundary), `regex` (raw)
- Validates regex at compile time, skips invalid patterns
- **Critical finding**: This is where user-created playbook rules become regex matchers. If we migrate to AI-native playbooks, this method needs the biggest change.
- **Security note** (line 663-664): Raw regex patterns from users are compiled directly. No ReDoS protection. A malicious user could craft a catastrophic backtracking regex like `(a+)+$` that hangs the server.

### Summary Classification

| Classification | Count | Rules |
|---|---|---|
| **KEEP** | 5 | auto_renewal, reverse_engineering, governing_law, notice_provision, confidentiality_term (as pre-filter) |
| **REPLACE-WITH-AI** | 8 | unlimited_liability, unilateral_termination, broad_indemnification, ip_assignment, assignment_restriction, non_compete, exclusive_dealing, non_solicitation (keep as pre-filter) |
| **REMOVE** | 0 | None — all rules serve a purpose, but 8 should transition to AI-primary with regex as optional pre-filter |

### Critical Findings

1. **ReDoS vulnerability** (line 663-664): `from_playbook_rules()` compiles user-supplied regex patterns without ReDoS protection. A malicious regex could cause CPU exhaustion. This is a P0 security issue.

2. **Semicolon as sentence boundary** (line 588): Legal contracts use semicolons within clauses for list items. Using `;` as a sentence terminator truncates clause context, leading to incomplete match text being sent to AI for verification.

3. **13 rules, not 16**: The PRD mentions "16 built-in rules" but DEFAULT_RULES has only 13. The expanded library (rules_library.py, 75 rules) is the actual default.

4. **SmartRule escalation is order-dependent** (lines 463-490): De-escalation is checked FIRST and takes priority. If both escalation and de-escalation patterns match, de-escalation wins. This is intentional but not documented for users.

5. **from_playbook_rules uses rule.clause_type as the name** (line 705): `name=rule.clause_type` — this means the rule name shown to users is the clause_type, not the rule's actual title. Could cause confusing UI labels.

6. **Normalized text dedup is too aggressive** (lines 556-566): If two genuinely different clauses have the same normalized text (e.g., two "governing law" clauses in different sections), only the first is kept. This is a false-negative risk for contracts with repeated standard language.
